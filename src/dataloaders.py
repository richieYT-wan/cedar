import torch
from torch.utils.data import Dataset, DataLoader
from src.data_processing import encode_batch_weighted


class CedarDataset(Dataset):
    """
    Takes as input a dataframe
    MAYBE NOT NEEDED AFTER ALL
    """
    def __init(self):
        pass
